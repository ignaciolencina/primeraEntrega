{
  "name": "primeraentrega",
  "version": "1.0.0",
  "description": "Primera entrega de backend 1 CoderHouse",
  "main": "./src/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.cjs --minify",
    "start": "node --env-file=.env dist/index.cjs",
    "dev": "node --watch --env-file=.env src/index.js",
    "lint": "eslint . --fix"
  },
  "author": "Ignacio Lencina",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
